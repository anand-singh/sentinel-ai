steps:
  # Build with Maven
  - name: 'maven:3.9-eclipse-temurin-17'
    entrypoint: mvn
    args: ['clean', 'install', '-DskipTests']
    dir: 'sentinel-ai-platform'

  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/sentinel-ai-images/sentinel-api:$COMMIT_SHA'
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/sentinel-ai-images/sentinel-api:latest'
      - '-f'
      - 'sentinel-ai-platform/sentinel-ai-api/Dockerfile'
      - 'sentinel-ai-platform/sentinel-ai-api'

  # Push Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/sentinel-ai-images/sentinel-api:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/sentinel-ai-images/sentinel-api:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'sentinel-api'
      - '--image=${_REGION}-docker.pkg.dev/$PROJECT_ID/sentinel-ai-images/sentinel-api:$COMMIT_SHA'
      - '--region=${_REGION}'
      - '--platform=managed'
      - '--allow-unauthenticated'

substitutions:
  _REGION: us-central1

options:
  machineType: 'N1_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: 1800s
